# Atelier PHP – Préparation de l’Environnement sous Windows 11

## Objectif

## 1. Introduction à PHP

- **PHP (PHP : Hypertext Preprocessor)** est un **langage de script côté serveur** intégré au **HTML**, conçu pour créer du contenu web dynamique.  

- Créé par **Rasmus Lerdorf** en 1994, il fonctionne sur toutes les grandes plateformes et serveurs (**Apache**, **IIS**, **Nginx**, etc.).  

- **PHP 4 (2000)** - OOP très basique  
  **PHP 5 (2004)** -  OOP complet (révolution)  
  **Composer (2011)** -  Gestionnaire de dépendances

- Sa version actuelle est **PHP 8.4.3** (janvier 2025).

- PHP permet de :
  
  - Gérer le contenu dynamique et les sessions.
  
  - Accéder aux bases de données comme **MySQL**, **PostgreSQL**, **Oracle**, **SQL Server**.
  
  - Construire des applications Web complètes : CMS, e-commerce, systèmes de connexion, API REST.

- PHP est open-source, multiplateforme, rapide et soutenu par une large communauté.

---

## 2. Composants d’un Environnement PHP local

Un environnement complet comprend :

- **Serveur Web :** généralement **Apache** ou **nginx**.

- **Base de données :** **MySQL**, **MariaDB**,**Sqlite**  ou autres.

- **Interpréteur PHP :** exécute les scripts et génère la sortie HTML.

Sous **Windows 11**, il est possible d’installer ces composants séparément ou via un package complet comme **XAMPP** ,**WAMP** ou **MAMP**.

## 3. Objectif & prérequis

- Windows 10/11 (droits admin non requis).

- VS Code installé.

- **archive zip** fournit avec le support de formation contenant qui doit être mis au niveau`C:\phpsource\php` (déjà testée, avec `php.ini` validé et Xdebug fonctionnel).

- Il faut que `C:\phpsource\php` soit ajouté à la variable d'environement PATH
  
  ![](C:\PHP\images\0\0.png)

# 4. Installer VS Code + extensions utiles

Ouvrez VS Code → onglet **Extensions** et installez :

- **PHP Debug** (officiel Xdebug) – indispensable pour le pas-à-pas.

- **PHP Intelephense** – auto-complétion et intelligence de code.

- **PHP Server** – serveur HTTP local simple (pratique pour débuter).

- (Optionnel) **EditorConfig**, **GitLens**.

![](C:\PHP\images\0\1.png)

## 5. Créer un mini-projet de test (avec `.vscode/launch.json`)

Arborescence cible :

```bash
C:\work\php-xdebug-test\
  ├─ .vscode\
  │   └─ launch.json
  └─ index.php
```

Créer le fichier **C:\work\php-xdebug-test.vscode\launch.json** :

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "//": "Écoute Xdebug sur le port 9003 (local). Lancez F5 avant d'exécuter le code.",
      "name": "Écouter Xdebug (port 9003)",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "log": true
    }
  ]
}
```

Créer le fichier **C:\work\php-xdebug-test\index.php** :

```php
<?php
// Démo Xdebug : placez un point d'arrêt sur la ligne $msg = ...
$who = "world";

// Petite fonction pour tester la pile d'appels (stack)
function greet($name) {
    // Point d'arrêt ici : observez $name et $msg dans Variables
    $msg = strtoupper("Hello, $name!");
    return $msg;
}

// Appel
$result = greet($who);

// Affichage (utile en CLI et HTTP)
echo $result, PHP_EOL;

// (Optionnel) Vérifier que Xdebug est chargé
if (function_exists('xdebug_info')) {
    // Décommentez pour inspecter les infos Xdebug au navigateur
    // xdebug_info();
}
```

ommentaires :

- Le dossier doit s’appeler **.vscode** (avec le point).

- Pas de `pathMappings` nécessaires ici (code et exécution sur la même machine).

## 6. Déboguer en **CLI**

1. Ouvrez **le dossier** `C:\work\php-xdebug-test` dans VS Code.

2. Ouvrez `index.php` et **posez un breakpoint** (par ex. sur `$msg = ...`).

3. Démarrez le débogueur **F5** → “Écouter Xdebug (port 9003)”.

4. Dans un terminal **dans le dossier du projet** :

```php
php index.php
```

5. VS Code doit **s’arrêter** sur le breakpoint. Inspectez variables / pile d’appels, puis **F5** pour continuer. Sortie attendue :

```bash
HELLO, WORLD!
```

### 7. Déboguer en **HTTP** (extension PHP Server)

Option A — avec **PHP Server** (extension VS Code) :

- Clic droit sur `index.php` → **PHP Server: Serve project** → notez l’URL (ex: `http://127.0.0.1:3000/index.php`).

- Laissez le débogueur **en écoute** (F5 déjà lancé).

- Ouvrez l’URL dans le navigateur → VS Code **stoppe** sur le breakpoint.

Option B — serveur PHP intégré (sans extension) :

```bash
php -S 127.0.0.1:8000 -t C:\work\php-xdebug-test
```

Puis allez sur `http://127.0.0.1:8000/`.

## 8. Validation rapide (checklist)

- `php --version` → affiche Xdebug **sans erreurs**.

- **CLI** : `php index.php` → arrêt au breakpoint.

- **HTTP** : PHP Server (ou `php -S`) → arrêt au breakpoint.

- `php -m | findstr /I xdebug` → affiche `xdebug`.

### 9. Pannes courantes & correctifs

- **Port 9003 occupé** (erreur EADDRINUSE) : trouver/terminer le process.

```bash
netstat -ano | findstr :9003
tasklist | findstr <PID>
taskkill /PID <PID> /F
```

- Ou basculer sur un autre port (ex: 9005) **dans `php.ini` et `launch.json`**.

- **Breakpoint ignoré** : lancez F5 **avant** d’exécuter `php` ou de charger la page.

- **Toujours pas d’arrêt** : confirmez que le **bon** `php.ini` est chargé :

```bash
php --ini
```

et que `xdebug.start_with_request=yes` est actif.

---

## Optionel  Lier Apache à ton PHP (module Apache)

### 1.1 Vérifier le module PHP pour Apache

Dans `C:\phpsource\php\`, tu dois avoir **`php8apache2_4.dll`** (build **TS/ZTS** — c’est ton cas).  
Si le fichier est présent, continue avec **mod_php** (le plus simple). Sinon, dis-le et je te donne la variante **FastCGI (mod_fcgid)**.

### 1.2 Activer PHP dans `httpd.conf`

Ouvre `C:\Apache24\conf\httpd.conf` et ajoute (en fin de fichier, ou dans la section modules) :

dans Créer le fichier **C:\Apache24\conf\httpd.conf** 

s'assurer que le Apache pointe sur le dossier ou se trouve le php

```bash
# PHP via module Apache (mod_php)
LoadModule php_module "C:/phpsource/php/php8apache2_4.dll"
PHPIniDir "C:/phpsource/php"

# Faire reconnaître les .php
AddType application/x-httpd-php .php
AddType text/html .php
DirectoryIndex index.php index.html

# Apache écoute déjà sur 8080 (existant dans ton httpd.conf)
Listen 8080

# (Optionnel) DocumentRoot par défaut
# DocumentRoot "C:/Apache24/htdocs"
# <Directory "C:/Apache24/htdocs">
#   Options Indexes FollowSymLinks
#   AllowOverride All
#   Require all granted
# </Directory>
```

> **Remarque **: utilise des **slashes `/`** dans les chemins Apache.

Redémarrer Apache

```bash
C:\Apache24\bin\httpd.exe -k restart
```

Vérifier qu’il écoute :

```bash
netstat -ano | findstr :8080
```

### Vérifier le couple Apache ↔ PHP ↔ Xdebug

### Page de test PHP

Crée un petit projet accessible par Apache. Si tu utilises `htdocs` :

Créer le fichier **C:\Apache24\htdocs\index.php** :

```php
<?php
// Page de test : PHP + Xdebug + Apache
echo "OK Apache+PHP<br>";
echo "PHP Version: " . PHP_VERSION . "<br>";

if (function_exists('xdebug_info')) {
    echo "Xdebug chargé<br>";
    // xdebug_info(); // décommente si tu veux la page d’info Xdebug
}
```

Ouvre `http://127.0.0.1:8080/` — tu dois voir “OK Apache+PHP” + version.  
Si erreur 500, regarde `C:\Apache24\logs\error.log`.

## VS Code : config débogage Xdebug (via Apache)

Si ton code est **dans `C:\Apache24\htdocs`** et que VS Code ouvre **ce dossier**, pas besoin de mappage.  
Crée/valide la configuration suivante :

Créer le fichier **C:\Apache24\htdocs\.vscode\launch.json** :

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "//": "Écoute Xdebug (Apache déclenche, VS Code attend). Lance F5 puis recharge la page http://127.0.0.1:8080/.",
      "name": "Écouter Xdebug (Apache, port 9003)",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "log": true
    }
  ]
}
```

> Si ton projet **n’est pas** sous `htdocs` (ex : `C:\work\mon-projet`) mais qu’Apache sert **ce dossier** comme DocumentRoot, pas besoin de `pathMappings`.  
> Si Apache sert un **autre chemin** que celui ouvert dans VS Code, ajoute un mapping :

Créer le fichier **C:\work\mon-projet.vscode\launch.json** :

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Écouter Xdebug (Apache, mapping)",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "log": true,
      "pathMappings": {
        "C:\\Apache24\\htdocs": "C:\\work\\mon-projet"
      }
    }
  ]
}
```

### Test pas-à-pas côté Apache

Créer le fichier **C:\Apache24\htdocs\test.php** :

```php
<?php
// Pose un breakpoint sur la ligne $msg = ...
$who = "world";
function greet($name) {
    $msg = strtoupper("Hello, $name!");
    return $msg;
}
echo greet($who);
```

- Dans VS Code (dossier du projet), place un **breakpoint** sur `$msg = ...`.

- Appuie **F5** (sélectionne “Écouter Xdebug (Apache, port 9003)”).

- Dans le navigateur, ouvre **http://127.0.0.1:8080/test.php**

- VS Code doit **s’arrêter** sur le breakpoint (pile d’appels + variables).

## Éviter les conflits de ports et d’outils

- **Ne lance pas** en même temps le **serveur PHP intégré** (`php -S ...`) ou l’extension **PHP Server** sur le **même projet** quand tu testes **Apache**.

- Si VS Code affiche `EADDRINUSE: :::9003` :

```bash
netstat -ano | findstr :9003
tasklist | findstr <PID>
taskkill /PID <PID> /F
```

## Option VirtualHost (recommandé pour ateliers multi-projets)

Créer le fichier **C:\Apache24\conf\extra\httpd-vhosts.conf** :

```bash
<VirtualHost *:8080>
  ServerName php.test
  DocumentRoot "C:/work/php-xdebug-test/public"
  <Directory "C:/work/php-xdebug-test/public">
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
  </Directory>
</VirtualHost>
```

Dans `httpd.conf`, assure que :

```bash
Include conf/extra/httpd-vhosts.conf
```

(Option) Ajoute dans `C:\Windows\System32\drivers\etc\hosts` :

```bash
127.0.0.1   php.test
```

Tu navigues alors sur `http://php.test:8080/`.  
Pense au `pathMappings` si le dossier ouvert dans VS Code ≠ `DocumentRoot`.

---

Excellent choix ✅  
Pour tes **participants**, un environnement **MAMP** est plus simple que la configuration Apache + PHP manuelle, tout en gardant **le débogage Xdebug** et **la compatibilité VS Code**.  
Voici donc un **tutoriel complet et pédagogique** pour installer, configurer et déboguer avec **MAMP + VS Code + Xdebug**, étape par étape.

---

# 🎓 Atelier : Configuration complète de MAMP + VS Code + Xdebug

---

## 🎯 Objectif

Mettre en place un environnement PHP local complet et stable :

- **Apache + MySQL + PHP** via **MAMP**

- **Débogage pas-à-pas** dans **Visual Studio Code**

- **Extensions utiles** pour le développement PHP moderne

---

## 🧱 1. Installation de MAMP

### Étape 1.1 – Télécharger

- Rendez-vous sur : [Downloads - MAMP &amp; MAMP PRO](https://www.mamp.info/en/downloads/)

- Téléchargez **MAMP (pas MAMP PRO)** pour Windows.

### Étape 1.2 – Installer

- Lancez l’installeur (droits admin requis une seule fois).

- Acceptez les options par défaut :
  
  ```
  C:\MAMP
  C:\MAMP\htdocs
  ```

- À la fin, MAMP propose de démarrer : acceptez.

### Étape 1.3 – Vérification

- Ouvrez le panneau MAMP → bouton **Start Servers**.

- Dans ton navigateur, ouvre :  
  👉 `http://localhost:8888`  
  Tu dois voir la page d’accueil de MAMP.

---

## ⚙️ 2. Configuration de PHP dans MAMP

### Étape 2.1 – Choisir la version PHP

- Ouvre MAMP → **Preferences → PHP**

- Choisis la version **PHP 8.x** (par ex. 8.2 ou 8.4 selon ton besoin).

### Étape 2.2 – Trouver le chemin vers le `php.ini`

Les fichiers PHP de MAMP se trouvent ici :

```
C:\MAMP\bin\php\php8.x.x\
```

Exemple :

```
C:\MAMP\bin\php\php8.2.0\
```

Ouvre ce dossier, tu verras un fichier :

```
php.ini
```

### Étape 2.3 – Activer Xdebug

#### a) Vérifier la présence de Xdebug

Dans :

```
C:\MAMP\bin\php\php8.x.x\ext\
```

tu dois trouver une DLL nommée :

```
php_xdebug.dll
```

ou

```
php_xdebug-3.x.x-8.x-vs16-x64.dll
```

#### b) Éditer le `php.ini`

Ouvre `C:\MAMP\bin\php\php8.x.x\php.ini`  
Vers la fin du fichier, ajoute (ou adapte) les lignes suivantes :

Créer le fichier **C:\MAMP\bin\php\php8.3.1\php.ini** :

```ini
zend_extension="C:\MAMP\bin\php\php8.3.1\ext\php_xdebug.dll"
xdebug.mode=debug,develop
xdebug.start_with_request=yes
xdebug.client_host=127.0.0.1
xdebug.client_port=9003
xdebug.idekey=VSCODE
```

#### c) Redémarre MAMP

Ferme et relance MAMP → **Start Servers**.

---

## 🧪 3. Vérifier le bon fonctionnement de PHP + Xdebug

Créer le fichier **C:\MAMP\htdocs\info.php** :

```php
<?php
phpinfo();
```

Ouvre ton navigateur sur :

```
http://localhost:8888/info.php
```

➡️ Vérifie que tu vois un bloc “Xdebug” dans la page.  
Si oui : **Xdebug est fonctionnel ✅**  
Sinon : vérifie le chemin `zend_extension=` dans le `php.ini`.

---

## 💡 4. Configuration de Visual Studio Code

### Étape 4.1 – Extensions recommandées

Installe depuis le Marketplace :

- **PHP Debug** (par Felix Becker)

- **PHP Intelephense**

- **PHP Server**

- **EditorConfig for VS Code** *(optionnel)*

### Étape 4.2 – Ouvrir ton projet MAMP

- Ouvre dans VS Code le dossier :
  
  ```
  C:\MAMP\htdocs\monprojet
  ```

- Crée un fichier `index.php` de test.

Créer le fichier **C:\MAMP\htdocs\monprojet\index.php** :

```php
<?php
// Exemple simple pour tester le débogage
$message = "Hello MAMP!";
echo $message;

// Appel fonctionnel
function add($a, $b) {
    $sum = $a + $b; // mets un breakpoint ici
    return $sum;
}

echo "<br>Résultat : " . add(5, 7);
```

---

## 🧩 5. Configurer VS Code pour Xdebug

Créer le dossier **C:\MAMP\htdocs\monprojet.vscode**  
et y placer le fichier suivant :

Créer le fichier **C:\MAMP\htdocs\monprojet.vscode\launch.json** :

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Écouter Xdebug (MAMP, port 9003)",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "log": true
    }
  ]
}
```

---

## 🧭 6. Lancer le débogage

### Étape 6.1 – Dans VS Code

- Ouvre ton projet `C:\MAMP\htdocs\monprojet`

- Place un **breakpoint** sur la ligne :
  
  ```php
  $sum = $a + $b;
  ```

- Appuie sur **F5** (choisis “Écouter Xdebug (MAMP, port 9003)”)

### Étape 6.2 – Dans le navigateur

Ouvre :

```
http://localhost:8888/monprojet/index.php
```

🎯 Résultat attendu :

- VS Code **s’arrête sur ton point d’arrêt**

- Tu peux inspecter `$a`, `$b`, `$sum` dans le volet “Variables”

---

## 🧹 7. Résolution des erreurs fréquentes

| Problème                    | Cause                              | Solution                                                |
| --------------------------- | ---------------------------------- | ------------------------------------------------------- |
| *Xdebug ne se connecte pas* | Mauvais chemin `zend_extension`    | Vérifie que le chemin vers `php_xdebug.dll` est correct |
| *Port déjà utilisé (9003)*  | Autre session Xdebug active        | Ferme VS Code, redémarre MAMP                           |
| *Breakpoint ignoré*         | L’écoute Xdebug n’est pas démarrée | Appuie sur F5 avant de recharger la page                |
| *Aucune page ne s’affiche*  | PHP plante au démarrage            | Consulte `C:\MAMP\logs\php_error.log`                   |

---

## 🧠 8. Option avancée : test en ligne de commande (CLI)

Créer le fichier **C:\MAMP\htdocs\monprojet\cli_test.php** :

```php
<?php
echo "Débogage CLI\n";
$x = 10;
$y = 20;
$z = $x + $y; // breakpoint ici
echo "Résultat : $z\n";
```

Dans un terminal :

```bash
"C:\MAMP\bin\php\php8.x.x\php.exe" cli_test.php
```

👉 Le débogueur VS Code s’arrête également sur le point d’arrêt.

---

## 🧩 9. Récapitulatif pédagogique

| Composant       | Rôle                       | Emplacement                                    |
| --------------- | -------------------------- | ---------------------------------------------- |
| **Apache**      | Serveur web intégré à MAMP | `C:\MAMP\bin\apache`                           |
| **PHP**         | Moteur d’exécution         | `C:\MAMP\bin\php\php8.x.x`                     |
| **MySQL**       | Base de données            | `C:\MAMP\bin\mysql`                            |
| **htdocs**      | Racine des projets web     | `C:\MAMP\htdocs`                               |
| **php.ini**     | Configuration PHP/Xdebug   | `C:\MAMP\bin\php\php8.x.x\php.ini`             |
| **launch.json** | Lien VS Code ↔ Xdebug      | `C:\MAMP\htdocs\monprojet\.vscode\launch.json` |

---

## 🚀 10. Validation finale

1. `php --version` → version MAMP + Xdebug

2. Naviguer vers `http://localhost:8888/monprojet/index.php`

3. VS Code arrête le code au breakpoint

4. Variables visibles → **débogage opérationnel 🎯**

---

# Optionel

## ⚙️ 1. Comprendre les différences entre le PHP “portable” et celui de MAMP

| Élément              | Ton installation portable (`C:\phpsource\php`) | MAMP (`C:\MAMP\bin\php\php8.3.1`)  |
| -------------------- | ---------------------------------------------- | ---------------------------------- |
| Chemin du moteur PHP | `C:\phpsource\php\php.exe`                     | `C:\MAMP\bin\php\php8.3.1\php.exe` |
| Fichier de config    | `C:\phpsource\php\php.ini`                     | `C:\MAMP\bin\php\php8.3.1\php.ini` |
| Extensions           | `C:\phpsource\php\ext`                         | `C:\MAMP\bin\php\php8.3.1\ext`     |
| Serveur Web          | Apache externe (ou intégré PHP)                | Apache intégré MAMP                |
| Logique              | identique                                      | identique                          |

✅ Donc **la structure et les directives sont les mêmes** : ton `php.ini` est réutilisable à 95 %.  
Seuls les **chemins** changent.

---

## 🧩 2. Étapes concrètes d’adaptation

### Étape 1 : copier ton php.ini

Copie ton fichier validé :

```
C:\phpsource\php\php.ini
```

vers :

```
C:\MAMP\bin\php\php8.3.1\php.ini
```

(remplace l’ancien, après sauvegarde éventuelle de l’original → `php.ini.bak`)

---

### Étape 2 : ajuster les chemins internes

Ouvre `C:\MAMP\bin\php\php8.3.1\php.ini` et modifie uniquement :

```ini
extension_dir="C:\MAMP\bin\php\php8.3.1\ext"
zend_extension="C:\MAMP\bin\php\php8.3.1\ext\php_xdebug-3.3.2-8.3-vs16-x64.dll"
```

> ⚠️ Le nom exact de la DLL dépend de la version téléchargée (voir ci-dessous).

---

### Étape 3 : récupérer le bon Xdebug

1. Va sur [Xdebug: Downloads](https://xdebug.org/download)

2. Sélectionne :
   
   - **PHP 8.3 (x64)**
   
   - **Thread Safe (ZTS)**
   
   - **Visual Studio 16 (VS16)** – c’est celui utilisé par MAMP

3. Télécharge le fichier `.dll` correspondant, par ex.
   
   ```
   php_xdebug-3.3.2-8.3-vs16-x86_64.dll
   ```

4. Place-le dans :
   
   ```
   C:\MAMP\bin\php\php8.3.1\ext\
   ```

---

### Étape 4 : conserver ta configuration Xdebug

Ton bloc existant fonctionne tel quel :

```ini
zend_extension="C:\MAMP\bin\php\php8.3.1\ext\php_xdebug-3.3.2-8.3-vs16-x64.dll"
xdebug.mode=develop,debug
xdebug.start_with_request=yes
xdebug.client_host=127.0.0.1
xdebug.client_port=9003
xdebug.idekey=VSCODE
```

Rien d’autre à changer.

---

### Étape 5 : vérifier dans le terminal MAMP

Ouvre un terminal et exécute :

```bash
"C:\MAMP\bin\php\php8.3.1\php.exe" --version
```

Résultat attendu :

```
PHP 8.3.1 (cli)
Zend Engine v4.3.1
with Xdebug v3.3.2
```

Puis :

```bash
"C:\MAMP\bin\php\php8.3.1\php.exe" -m | findstr /I xdebug
```

→ doit afficher `xdebug`.

---

## ✅ 3. Vérification via navigateur

Créer le fichier **C:\MAMP\htdocs\info.php** :

```php
<?php
phpinfo();
```

Dans ton navigateur :

```
http://localhost:8888/info.php
```

➡️ Cherche la section **Xdebug** → tu verras la version et les paramètres (port, host…).

---

## 🧠 4. Points à retenir

| Ce qu’on garde                                       | Pourquoi                        |
| ---------------------------------------------------- | ------------------------------- |
| Ton `php.ini` nettoyé                                | configuration stable et validée |
| Les mêmes extensions (curl, gd, mbstring, pdo_mysql) | MAMP les fournit aussi          |
| Les mêmes directives Xdebug                          | Xdebug 3 reste compatible       |
| Le port 9003                                         | standard VS Code                |
| Le dossier `.vscode/launch.json`                     | inchangé                        |

---

## 🧩 5. Exemple de configuration finale minimale pour MAMP 8.3.1

Créer le fichier **C:\MAMP\bin\php\php8.3.1\php.ini** :

```ini
[PHP]
extension_dir="C:\MAMP\bin\php\php8.3.1\ext"
extension=curl
extension=gd
extension=mbstring
extension=pdo_mysql

zend_extension="C:\MAMP\bin\php\php8.3.1\ext\php_xdebug-3.3.2-8.3-vs16-x64.dll"
xdebug.mode=develop,debug
xdebug.start_with_request=yes
xdebug.client_host=127.0.0.1
xdebug.client_port=9003
xdebug.idekey=VSCODE
```

---

## 🧩 6. Débogage VS Code (identique à ton tutoriel précédent)

Créer le fichier **C:\MAMP\htdocs\monprojet.vscode\launch.json** :

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Écouter Xdebug (MAMP 8.3.1)",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "log": true
    }
  ]
}
```

Puis :

- F5 dans VS Code → “Écouter Xdebug”

- Ouvre `http://localhost:8888/monprojet/index.php`

- Le breakpoint est atteint 🎯

---

Souhaites-tu que je te génère maintenant **un package .zip “MAMP 8.3.1 + php.ini prêt à l’emploi”** pour distribuer directement à tes stagiaires (avec l’arborescence `htdocs` + `.vscode` déjà prêts) ?
